# Tên của quy trình tự động
name: Deploy React App to GitHub Pages

# Quy trình này sẽ chạy mỗi khi bạn "push" code lên nhánh "main"
on:
  push:
    branches: ["main"] # Hoặc "master" nếu nhánh chính của bạn là master
  workflow_dispatch:

# Cấp quyền cho workflow để ghi vào repository
permissions:
  contents: read
  pages: write
  id-token: write

# Các công việc cần thực hiện
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # Chạy trên một máy ảo Ubuntu
    steps:
      # 1. Lấy mã nguồn từ repository về
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # 2. Cài đặt Node.js để có thể chạy npm
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Sử dụng Node.js phiên bản 20

      # 3. Cài đặt các thư viện cần thiết
      - name: Install dependencies 📦
        run: npm install

      # 4. Build dự án, tạo ra folder `dist` chứa website tĩnh
      - name: Build project 🏗️
        run: npm run build

      # 5. Cấu hình để deploy lên GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v5
        
      # 6. Tải lên các file đã build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' # Đường dẫn đến folder build
          
      # 7. Deploy!
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
